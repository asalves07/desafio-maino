<%= form_with(model: team, local: true) do |form| %>
  <% if team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
        <% team.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :coach_id %>
    <%= form.text_field :coach_id %>
  </div>

  <div class="field">
    <%= form.label :pokemon %>
    <%= form.collection_select(:subjct_id) %>
  </div>

  <div id='pokemons'>
    <%= form.fields_for :pokemons do |pokemon| %>
      <%= render partial: "pokemon_fields", locals: {f: pokemon} %>
    <% end %>
    <%= link_to_add_association "Adicionar pokemons", form, :pokemons %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
